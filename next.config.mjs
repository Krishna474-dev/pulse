/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hides the floating dev-tools badge. Development only - it never ships in a
  // production build. Set back to true to get the issues panel back.
  devIndicators: false,
};

export default nextConfig;
